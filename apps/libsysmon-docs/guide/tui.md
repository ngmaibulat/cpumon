# The dashboard has moved

`etop` — the full-screen dashboard built on these collectors — has its own
documentation site now, because it is its own package with its own release
cycle.

**→ [etop documentation](https://github.com/ngmaibulat/cpumon/tree/main/packages/etop)**

```sh
npx @aibulat/etop
```

Node 22 or newer. `libsysmon` itself still supports Node 18.
